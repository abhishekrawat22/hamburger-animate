const menuButton = document.getElementsByClassName('menu-btn')[0];

menuButton.addEventListener('click', function() {
//   Toggling 'animate' class to apply desired animation when button is clicked.
  menuButton.classList.toggle('animate');
});
